# SecPipeline 프로젝트 현황 및 교수님 평가 기준 분석 보고서

## 1. 현재 상황 분석
- **기존 목표**: 5가지 의도적 보안 취약점이 삽입된 간단한 Flask 스켈레톤 앱(StudyLog)을 바탕으로, GitHub Actions 기반 5-Gate DevSecOps(Gitleaks, pip-audit, Semgrep/Bandit, Trivy, hadolint) 탐지 및 조치 과정 증명.
- **현재 진행 상황 (`0324_개발.pdf`)**: Flask 스켈레톤 코드 작성, 의도적 취약점 삽입, 그리고 GitHub Actions를 통한 Gate 1~3(Secret, Dependency, SAST)까지의 구축 및 탐지 테스트(Red Run) 분석 완료.

## 2. 교수님 평가 기준 대비 갭(Gap) 분석 및 보완점
교수님께서 제시한 평가 기준과 추가 점수 항목, 그리고 수업 시간에 배운 실무적 내용(테스트 자동화, Git/GitHub 활용, 로깅 등)을 고려할 때 다음 부분들이 크게 부족합니다.

1. **애플리케이션 완성도 및 복잡도 (가산점 15점 + 기본 10점)**
   - **현황**: 현재의 StudyLog 앱은 취약점을 시연하기 위해 임의로 만든 최소한의 뼈대(CRUD만 존재)입니다.
   - **보완**: 평가 기준에 "웹 애플리케이션을 제대로 만드는 것"이 포함되었고 복잡도가 가산점 요소이므로, 사용자님이 기존에 **"잘 만들어둔 웹 애플리케이션" 코드를 현재 프로젝트에 이식(Migration)**하여 비즈니스 로직과 복잡도를 높이는 것이 매우 유리합니다.
   
2. **테스트 자동화 및 커버리지 평가 (수업 내용 활용 + 가산점)**
   - **현황**: `test_auth.py` 등에 단순한 HTTP 상태 코드(200 OK)만 확인하는 기초적인 테스트 3개만 존재합니다.
   - **보완**: 수업에서 강조한 **TDD 사이클**, `pytest`, `httpx`를 이용한 꼼꼼한 단위/API 통합 테스트(조건/분기 커버리지 포함)가 필요합니다. `conftest.py`를 활용한 Setup/Teardown 구성과 `coverage` 패키지를 통한 커버리지 측정, 그리고 **"PR에 커버리지 결과를 댓글로 달아주는 GitHub Actions"** 추가가 필수적입니다.
   
3. **협업 도구(Git/GitHub)의 적극적 활용 (15점)**
   - **현황**: 단순 `git commit` 및 `push origin main` 방식으로만 개발 이력이 남겨져 있습니다.
   - **보완**: 실무적인 협업 흐름을 보여주어야 합니다.
     - 기능 추가나 버그 수정 시 **Issue 생성 -> 브랜치 분기(`fix/..`, `feat/..`) -> 코드 작성 -> PR(Pull Request) 생성 -> CI 통과 후 Merge**의 흐름(GitHub Flow)을 기록으로 남겨야 합니다.
     - 수업에서 언급된 **"자동 이슈 코멘터 봇"** 및 **"코드 리뷰 봇"** GitHub Actions 워크플로우를 추가하여 자동화 수준(10점)을 높여야 합니다.

4. **로깅(Logging) 적용 (수업 내용 활용)**
   - **현황**: 애플리케이션 로깅이 거의 없습니다.
   - **보완**: 앱의 주요 행위(에러 발생, DB 접근, 주요 API 호출 등)를 체계적으로 남길 수 있는 Python `logging` 모듈 적용이 필요합니다.

5. **Docker 및 배포 파이프라인 완성 (10점)**
   - **현황**: Dockerfile 작성과 스캔까지만 기획되어 있고, 배포 부분이 적용되지 않았습니다.
   - **보완**: 기존 계획된 Render 배포 흐름을 CI/CD의 마지막 Job(`deploy`)으로 붙여, 코드가 Main에 Merge되면 자동으로 Render로 배포되게 구성해야 합니다.

---

## 3. Antigravity Agent와 함께 진행할 단계별 프롬프트 가이드
사용자님이 기존에 만든 웹을 가져오고 위의 모든 요구사항을 충족시키기 위해, 다음 프롬프트들을 차례대로 복사하여 Antigravity에게 지시하시면 됩니다.

### [1단계] 기존 웹 애플리케이션 이식 및 로깅 추가
```text
제가 이전에 제대로 만들어둔 웹 애플리케이션 소스 코드가 있습니다. 이 코드를 현재 SecPipeline 프로젝트 구조에 맞게 이식하려고 합니다.
1. 기존 앱의 라우팅과 로직을 현재 프로젝트 구조(`app/routes`, `app/templates` 등)에 맞게 통합해 주세요. 
2. 통합된 애플리케이션에 Python `logging` 모듈을 적용하여 주요 이벤트(회원가입, 로그인, 에러 등)와 API 요청에 대한 로깅 시스템을 구축해 주세요.
3. 기존 SecPipeLine에서 계획했던 의도적 취약점(SQLi, XSS 등)이 새 앱에서도 잘 테스트될 수 있도록 필요한 경우 적절히 배치해 주세요.
```

### [2단계] 테스트 자동화 환경(Pytest, Coverage) 구축
```text
수업 시간에 배운 테스트 자동화를 적용하려고 합니다.
1. `tests/conftest.py`를 생성하여 Setup 및 Teardown(DB 초기화, 테스트 클라이언트 생성 등) 환경을 구성해 주세요.
2. `pytest`와 `httpx`를 활용하여 새로 이식한 애플리케이션의 핵심 비즈니스 로직에 대한 단위 테스트와 API 테스트 코드를 작성해 주세요. (TDD의 1단계 Red, 2단계 Green 흐름을 반영할 수 있도록 꼼꼼한 테스트 케이스를 설계해 주세요)
3. `pytest-cov` 패키지를 추가하여 테스트 커버리지를 측정할 수 있도록 `requirements.txt`와 관련 설정을 업데이트해 주세요.
```

### [3단계] GitHub 자동화 (이슈 코멘터, PR 커버리지 등) 추가
```text
협업 및 자동화 평가 기준을 충족하기 위해 GitHub Actions 워크플로우를 추가로 설정해 주세요.
1. 이슈가 열렸을 때 자동으로 인사말과 안내 코멘트를 달아주는 '자동 이슈 코멘터 봇' 워크플로우(`.github/workflows/issue_commenter.yml`)를 작성해 주세요.
2. PR이 생성되었을 때, 앞서 만든 pytest 테스트를 실행하고, 코드 커버리지(Coverage) 측정 결과를 PR 댓글(Comment)로 자동으로 요약해서 달아주는 액션을 구성해 주세요.
3. 이를 기존 DevSecOps 파이프라인과 충돌하지 않도록 잘 통합해 주세요.
```

### [4단계] DevSecOps 파이프라인 완성 및 배포 (Docker + Render)
```text
교수님의 전체 파이프라인 완성도 평가를 위해, DevSecOps의 마지막 배포 단계를 완성하려고 합니다.
1. Dockerfile의 최적화(베스트 프랙티스 적용: non-root 사용자, 캐시 최적화, HEALTHCHECK 등)를 수행해 주세요.
2. 기존 `.github/workflows/devsecops.yml` 파일에 단위 테스트 실행 Job과, 테스트 및 보안 스캔을 모두 통과했을 때만 작동하는 Render 자동 배포(`deploy`) Job을 추가해 주세요.
3. 전체 흐름(Git -> CI/테스트 -> Docker 보안스캔 -> Render 배포)이 유기적으로 동작하도록 YAML을 검토해 주세요.
```
